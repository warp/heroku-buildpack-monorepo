#!/usr/bin/env bash
indent() {
  sed -u 's/^/      /'
}

BUILD_DIR="$1"

for dir in $TEST_DIRS; do
  (
    echo "Test compile ${dir}" | indent
    cd "${BUILD_DIR}/${dir}" && ./bin/test-compile
  )
done
